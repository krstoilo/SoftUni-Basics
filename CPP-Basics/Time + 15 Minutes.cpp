#include <iostream>

using namespace std;

int main () {

    int hours, minutes;
    cin >> hours >> minutes;

    /// if minutes < 45 - hours don't change and minutes are  + 15
    /// if minutes = 45 - hours change to + 1 and minutes == 0
    /// if minutes > 45 - hours + 1 and minutes






    return 0;
}
